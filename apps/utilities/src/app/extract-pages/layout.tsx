import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Extract Pages | Universal Document Utilities",
  description: "AI-Powered Universal Document Extract Pages engine.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
