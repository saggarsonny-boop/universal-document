import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Metadata Editor | Universal Document Utilities",
  description: "AI-Powered Universal Document Metadata Editor engine.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
