import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Version History | Universal Document Utilities",
  description: "AI-Powered Universal Document Version History engine.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
